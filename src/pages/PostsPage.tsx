import {PostsComponent} from "../components/posts-component/PostsComponent.tsx";

export const PostsPage = () => {
    return (
        <section>
            <PostsComponent/>
        </section>
    );
};